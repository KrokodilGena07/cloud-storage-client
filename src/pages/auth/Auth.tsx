import React, {FC} from 'react';
import {AuthForm} from '@/modules/authForm';

const Auth: FC = () => {
    return (
        <div>
            <AuthForm/>
        </div>
    );
};

export default Auth;